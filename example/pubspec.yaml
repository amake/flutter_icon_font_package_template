name: example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_icon_font_package_template:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
