name: example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_icon_font_package_template:
    path: ../

  flutter_svg: ^0.13.0

  fps:
    git:
      url: https://github.com/amake/fps.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/ei-heart.svg
